package com.una.system.mananger;

import java.util.UUID;

public class Test {
  //  public static void main(final String[] args) throws Exception {
  //    final String address = "127.0.0.1";
  //    final int port = 21;
  //    final String username = "test";
  //    final String password = "123456";
  //
  //    //    final String targetPath = "d:\\temp";
  //    final String targetPath = "/";
  //
  //    final Transmission instance = TransmissionUtil.Type.getInstance("ftp");
  //    instance.login(address, port, username, password);
  //
  //    instance.mkdirs(targetPath + "/aabbcc");
  //    instance.list(targetPath + "", null).forEach(f -> System.out.println(f.getName()));
  //    instance.rmdir(targetPath + "/aabbcc");
  //
  //    instance.setBufferSize(1024 * 1024 * 10);
  //
  //    try (FileInputStream inputStream = new FileInputStream("d:\\Love Me1.wav");) {
  //      instance.uploadFile(inputStream, targetPath + "/abc");
  //    }
  //    try (FileOutputStream outputStream = new FileOutputStream("d:\\temp\\def");) {
  //      instance.downloadFile("/abc", outputStream);
  //    }
  //
  //    instance.remove("/abc");
  //
  //    System.out.println(instance.isLogin());
  //    instance.logout();
  //    System.out.println(instance.isLogin());
  //  }

  public static void main(final String[] args) {
    System.out.println(UUID.randomUUID().toString());
    System.out.println(UUID.randomUUID().toString());
    System.out.println(UUID.randomUUID().toString());
    System.out.println(UUID.randomUUID().toString());
    System.out.println(UUID.randomUUID().toString());
    System.out.println(UUID.randomUUID().toString());
  }
}
