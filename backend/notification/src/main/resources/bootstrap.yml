spring:
  application:
    name: system-notification  # 应用程序名称，后面会在消费者中用到
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:18848
      config:
        server-addr: 127.0.0.1:18848
        # group: DEFAULT_GROUP
        # namespace: public
        file-extension: yaml